# ============================================================
# Install command:
#   uv pip install -r requirements.txt
# ============================================================

# Core FastAPI
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Environment
python-dotenv>=1.0.0

# Database
# motor 3.3.x is incompatible with newer pymongo (ImportError: _QUERY_OPTIONS).
motor>=3.7.1
pymongo>=4.9.0
certifi>=2024.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
python-multipart>=0.0.7
email-validator>=2.2.0
pyotp==2.9.0
qrcode[pil]>=7.4.2
cryptography>=41.0.0
PyJWT>=2.10.1

# HTTP & Utilities
# httpx>=0.28 required: supabase-py/gotrue call Client(proxy=...); httpx<0.26 only has proxies= and fails at import/init
httpx>=0.28.1
aiolimiter==1.1.0
# pydantic-settings pinned to >=2.4.0 (2.1.0 too old for fastapi/langgraph)
pydantic-settings>=2.4.0
psutil>=5.9.0
websockets>=15.0.1

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
markdown==3.5.1
pdfplumber==0.11.0
reportlab>=4.2.0

# Machine Learning & Vector Operations
# numpy: chromadb/sentence-transformers need <3.0.0, scipy>=1.16.1 needs >=2.3.0
numpy>=2.3.0,<3.0.0
pandas>=2.0.0
pyarrow>=14.0.0
scipy>=1.16.1
scikit-learn>=1.3.2

# Vector Databases
pinecone>=6.0.0
chromadb>=0.4.0
qdrant-client>=1.7.0
weaviate-client>=4.4.0

# AI/LLM Integration
openai>=1.0.0
groq>=0.4.0
sentence-transformers>=2.2.0
# torch & transformers pulled in automatically by sentence-transformers

# LangGraph (model configuration workflow)
langgraph>=0.2.0
langgraph-checkpoint-mongodb>=0.3.0

# Supabase
# postgrest removed: supabase>=2.29.0 bundles its own postgrest internally
supabase>=2.28.2

# Optional: Large packages, install separately if needed
# llama-cpp-python>=0.2.0
# accelerate  # optional, for local_model_loader GPU/optimized loading
